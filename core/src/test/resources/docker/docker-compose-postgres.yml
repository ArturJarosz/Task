version: '3.1'
services:
  db:
    image: postgres:14.1
    restart: always
    environment:
      POSTGRES_PASSWORD: test
      POSTGRES_USER: test
      POSTGRES_DB: taskdb
    ports:
      - "5432:5454"
